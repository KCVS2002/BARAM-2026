# BARAM 2026 — 풍력발전량 예측 AI 경진대회

태백 가덕산 풍력단지 3개 KPX 그룹의 2025년 시간별 발전량(kWh)을 LDAPS/GFS 기상예보로 예측하는 대회.
아래 규칙은 **실격 사유**와 직결되므로 모든 코드 작성 시 반드시 준수한다.

## 1. 데이터 누수 방지 (최우선 규칙)

- **예측기준시점**: 각 발전량 예측값은 해당 시점 이전에 실제로 활용 가능했던 정보만 사용한다.
  예보 데이터는 `data_available_kst_dtm`(전날 13:00 KST) 기준으로만 사용 가능 여부를 판단한다.
- 제공된 LDAPS/GFS 데이터는 이미 D-1 13:00 사용 가능 기준으로 정리되어 있음 → 같은 행의
  `forecast_kst_dtm`에 대응하는 예보를 그대로 쓰는 것은 안전하다.
- **금지**: 평가 기간(2025년)의 실제 발전량, SCADA, 비공개 운영 데이터, 사후 보정자료, 재분석자료(ERA5 등).
- **파생 변수 주의**: lag/rolling/집계/보간 피처를 만들 때 예측기준시점 이후 정보가 섞이면 안 된다.
  - 테스트 기간에는 발전량·SCADA lag 피처를 만들 수 없다 (2025년 실측이 없으므로).
  - 학습 데이터에서만 가능한 피처를 만들면 train/test 피처 불일치 → 만들지 않는다.
  - 전체 데이터(train+test)에 걸친 통계량(target encoding, 정규화 등)을 계산할 때 test 기간 정보가
    train 피처에 들어가는 것은 괜찮지만, test 실측/정답성 정보는 어떤 형태로도 사용 불가.
- **검증(CV)도 동일 원칙**: 시간 순서를 지키는 시계열 분할만 사용. 미래 데이터로 과거를 예측하는 검증 금지.

## 2. 외부 데이터 규칙

- 사용 가능 조건 (모두 충족): ① 법적 제약 없음 ② 저작권/라이선스/약관 준수 ③ 누구나 접근 가능한 공개 데이터
  ④ 누수 방지 규칙 준수 (각 예측값의 예측기준시점 이전에 생성·공개된 데이터만).
- 외부 데이터를 쓸 때마다 **출처, 수집 방법, 수집 시점, 라이선스, 활용 가능 시점 근거를 기록**해 둔다
  (2차 평가에서 재현·소명 의무 있음). `external_data/README.md`에 정리한다.
- 재분석자료(ERA5 등)는 사후 생성 자료이므로 **테스트 기간 추론에 사용 금지**.

## 3. 모델 규칙

- 사전학습 모델: 2026-07-05까지 가중치가 공개된 **오픈소스 라이선스**(상업적 이용 허용) 모델만 사용 가능.
- **외부 API 추론 금지** (OpenAI, HF Inference 등). 로컬에서 가중치를 직접 로드하는 방식만 허용.
- 언어는 Python만 사용.

## 4. 평가 산식 (최적화 목표)

- `Score = 0.5 × (1-NMAE) + 0.5 × FICR`, **실제 발전량 ≥ 설비용량 10%인 시간대만 평가**.
- NMAE: `|pred - actual| / 그룹 설비용량`의 평균 → 그룹별 계산 후 3그룹 평균.
- FICR: 오차율 ≤6% → 4원/kWh, ≤8% → 3원/kWh, 초과 → 0원. 실제 발전량 가중.
  → 단순 MAE 최소화가 아니라 **오차율을 6%/8% 밴드 안에 넣는 것**이 점수의 절반.
- 설비용량(kWh): group_1 = 21,600 / group_2 = 21,600 / group_3 = 21,000.
- 로컬 검증은 반드시 `평가_산식 코드.ipynb`의 `metric()` 함수를 그대로 사용해 측정한다.

## 5. 데이터 취급

- 모든 CSV는 `utf-8-sig` 인코딩으로 읽고 쓴다.
- 시간은 전부 KST, `kst_dtm`/`forecast_kst_dtm`은 **집계 구간의 종료 시각**.
- `kpx_group_3` 라벨은 2023년부터만 존재 (2022년은 NaN) → 그룹별로 학습 기간이 다름.
- 그룹 구성: group_1 = VESTAS 1~6호기, group_2 = VESTAS 7~12호기, group_3 = UNISON 1~5호기.
- 제출 파일: `forecast_id`, `forecast_kst_dtm` 변경 금지, 예측값만 채움, `utf-8-sig`로 저장.
- 예측값은 `[0, 그룹 설비용량]`으로 클리핑.
- 원본 데이터 파일(`Data/`)은 절대 수정하지 않는다.

## 6. 작업 실행 규칙

- 장시간 백그라운드 작업(수집·학습)은 반드시 `python -u ... 2>&1 | tee logs/<작업명>.log`로 실행해
  사용자가 `logs/` 폴더에서 실시간 진행도를 볼 수 있게 한다. grep을 끼울 땐 `--line-buffered`.
- 수집·학습 스크립트는 진행률(%)·ETA를 주기적으로 출력하고, 체크포인트 저장·이어받기를 지원한다.

## 7. 검증 원칙

- 제출 전 로컬 CV 점수를 항상 기록하고, 실험 로그(피처/모델/CV점수/리더보드점수)를 남긴다.
- 1일 제출 5회 제한 → 리더보드 제출은 로컬 CV에서 개선이 확인된 경우에만.
- **CV는 2024년 검증뿐이라 2025 분포이동을 못 본다** (v6에서 CV +0.004 ↔ LB -0.009 실증).
  파라미터/구조 변경은 LB 확인 전까지 잠정 채택으로 취급하고, LB 하락 시 즉시 원복한다.
  배깅 무작위성(subsample/colsample < 1)을 제거하는 설정은 분포이동에 취약하므로 지양.
- **rolling-origin CV는 최근성 가중 류의 이득을 구조적으로 부풀린다** (v8d에서 실증, LB -0.013):
  검증 구간이 학습 직후라 계절 연속성·NWP 버전을 공유. 시간 가중/lag 류 변경은 특히 의심할 것.
- **학습분포 적합도를 높이는 변경은 CV에서 이기고 LB에서 진다** (3회 실증: 정칙화 완화 -0.009,
  최근성 -0.013, 그룹 특이화 -0.0021). fold 일관성·계절 분산 확인으로도 못 거른다 — 모든 fold가
  연도 경계 안쪽이므로. 그룹 풀링(공유 학습)은 공통 패턴만 배우는 암묵적 정칙화이며 2025 전이의
  핵심 방어선. 특이화·완화 방향 실험은 원칙적으로 회피.
- **GBM 파라미터 축 전면 봉인** (#52): 정칙화 강화 방향도 LB 실패 (CV 6/6 전승 +0.005 ↔ LB -0.002).
  양방향 실증 실패 → 2024-내 CV는 파라미터 선택에 방향 불문 무정보. BASE_PARAMS 고정, 파라미터
  실험 금지. CV가 신뢰 가능한 것은 물리적 메커니즘이 있는 구조·의사결정층 변경뿐.
- **전이되는 것은 '구조'이지 '2024 측정 상수'가 아니다** (#54): 의사결정층이라도 2024에서 측정한
  보정량(캘리브레이션 격차, 가중 상수)을 이식하면 전이 실패 (α=0.7: CV FICR +0.0026 ↔ LB -0.0017).
  전이 성공 사례(FICR 최적화·재정렬·평활화)는 모두 2024 측정치 무의존 구조였음.
- **원자 폭·선명도 축 양방향 봉인** (#61 통일 이론): 선명화 3연속(reg·α·Vincent — 구조적 변경 포함)
  CV승 LB패, 넓히기 5연속 CV패. 2024 검증에서 보이는 '과분산'은 연도 경계 전이 오차의 마진이며
  2025의 추가 오차가 이를 정확히 소비 → 현 폭이 근사 최적. 폭/선명도를 바꾸는 어떤 변경도 금지.
  FICR은 조작 불가 — 진짜 예측 오차 감소(정보 우위 소스)만이 경로.
